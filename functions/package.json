{
  "name": "navespace-mcp-functions",
  "description": "Servidor MCP + asistentes (panel admin y taller) de naveSpace — herramientas de lectura y escritura sobre datos en vivo de la flota, museo, teatro y taller, con Gemini o Claude según AI_PROVIDER. Aprobar un presupuesto de taller (cobra a BankIn) queda fuera a propósito, solo disponible desde el panel.",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@google/generative-ai": "^0.21.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "cors": "^2.8.5",
    "firebase-admin": "^12.4.0",
    "firebase-functions": "^5.1.1",
    "zod": "^3.23.8"
  }
}
