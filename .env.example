# URL base de la API del backend spacecraftSystem (sin barra final)
# Local por defecto:
VITE_API_URL=http://localhost:8080/api

# URL base de la API del backend de taller (spacecraft-taller-backend, Python/FastAPI)
# Local por defecto:
VITE_TALLER_API_URL=http://localhost:8001/api
